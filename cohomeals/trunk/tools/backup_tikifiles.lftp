debug
# a bug somewhere makes tls fail 
set ftp:ssl-allow no
connect cohoecovillage.org
# create an alias in ~/.lftprc so this script can login to the
# ftp server, for example:
# alias cohotikilogin login tiki@cohoecovillage.org <password> 
cohotikilogin
mget -d img/coho_custom/*
mget -d files/*
#later do css/ and templates/ if modified

